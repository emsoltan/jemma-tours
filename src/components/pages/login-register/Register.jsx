import React from "react";
import { Link } from "react-router-dom";

const Register = () => {
  return (
    <div>
      Registerered customer ? try to <Link to="/login">Login</Link>
    </div>
  );
};

export default Register;
